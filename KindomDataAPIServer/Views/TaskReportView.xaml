<Window x:Class="KindomDataAPIServer.Views.TaskReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Closing="Window_Closing"
        Title="Task Report"
        Height="620"
        Width="980">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,6,8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="reportPathTextBox"
                     Margin="0,0,8,0"
                     IsReadOnly="True"
                     VerticalContentAlignment="Center" />
            <Button Grid.Column="1"
                    Width="72"
                    Height="26"
                    Margin="0,0,6,0"
                    Content="Latest"
                    Click="Latest_Click" />
            <Button Grid.Column="2"
                    Width="72"
                    Height="26"
                    Content="Open..."
                    Click="Open_Click" />
        </Grid>

        <TextBox x:Name="reportTextBox"
                 Grid.Row="1"
                 Margin="8,0,8,8"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 FontFamily="Consolas"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 HorizontalScrollBarVisibility="Disabled"
                 VerticalScrollBarVisibility="Visible" />
    </Grid>
</Window>
