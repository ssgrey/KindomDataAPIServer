<dx:ThemedWindow
    x:Class="KindomDataAPIServer.Views.AdvancedSettingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    Title="Advanced Settings"
    Width="420"
    Height="250"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel VerticalAlignment="Center" Orientation="Vertical">
            <StackPanel Height="32" Orientation="Horizontal">
                <Label
                    Width="190"
                    VerticalAlignment="Center"
                    Content="Well Header Batch Size" />
                <dxe:SpinEdit
                    Width="120"
                    MinValue="1"
                    MaxValue="1000000"
                    IsFloatValue="False"
                    EditValue="{Binding WellHeaderBatchSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Height="32" Orientation="Horizontal">
                <Label
                    Width="190"
                    VerticalAlignment="Center"
                    Content="Well Trajectory Batch Size" />
                <dxe:SpinEdit
                    Width="120"
                    MinValue="1"
                    MaxValue="1000000"
                    IsFloatValue="False"
                    EditValue="{Binding WellTrajectoryBatchSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Height="32" Orientation="Horizontal">
                <Label
                    Width="190"
                    VerticalAlignment="Center"
                    Content="Well Trajectory Upload Concurrency" />
                <dxe:SpinEdit
                    Width="120"
                    MinValue="1"
                    MaxValue="100"
                    IsFloatValue="False"
                    EditValue="{Binding WellTrajectoryUploadConcurrency, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Height="32" Orientation="Horizontal">
                <Label
                    Width="190"
                    VerticalAlignment="Center"
                    Content="Well Formation Batch Size" />
                <dxe:SpinEdit
                    Width="120"
                    MinValue="1"
                    MaxValue="1000000"
                    IsFloatValue="False"
                    EditValue="{Binding WellFormationBatchSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </StackPanel>
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <dx:SimpleButton
                Width="80"
                Height="26"
                Margin="0,0,8,0"
                Click="Ok_Click"
                Content="OK" />
            <dx:SimpleButton
                Width="80"
                Height="26"
                Click="Cancel_Click"
                Content="Cancel" />
        </StackPanel>
    </Grid>
</dx:ThemedWindow>
