# Kingdom 数据同步自适应设计方案

## 1. 目标

在不引入第三方框架的前提下，为 `SyncKindomDataViewModel.SyncCommandAction` 中的四类主要数据同步增加运行时自适应能力：

- 井分层（Formation）
- 井轨迹（Trajectory）
- 生产数据（Production）
- 测井曲线（WellLog）

优化优先级如下：

1. 优先提高单次上传 payload，减少小请求数量。
2. 根据 Kingdom SDK 读取表现调整本地读取批量。
3. 上传并发保持保守，第一版只允许自动降低，不自动增加。

接口响应中的业务 `failed` 项继续写入现有日志和任务报告，但不参与自适应判断。HTTP 超时、429、5xx 和网络重试属于运行环境或后端压力信号，仍参与保护性回退。

## 2. 现状依据

甲方旧版本日志 `app-2026-08-04-10.log` 显示四类数据的瓶颈不同：

| 数据类型 | 读取与上传特征 | 优化重点 |
| --- | --- | --- |
| 分层 | 读取约 1383.5 秒，上传请求累计约 11.1 秒，总 payload 约 0.235 MiB | 优先优化 Kingdom 读取批量，上传并发保持 1 至 2 |
| 轨迹 | 读取约 227.4 秒，上传请求累计约 1042.6 秒，2325 个请求、约 351 MiB | 优先增大单次 JSON payload，减少请求数 |
| 测井 | 读取约 1092.6 秒，上传请求累计约 5338.4 秒，24785 个请求、约 7956 MiB | 按 protobuf 字节和样点数组批，显著减少请求数 |
| 生产 | 该日志未包含生产同步 | 独立按历史记录、日数据条数和 JSON 字节控制 |

旧日志用于判断瓶颈类型，不能直接作为当前批量读取版本的绝对阈值。首版参数需要通过当前版本任务报告继续校准。

## 3. 总体架构

保留当前生产者/消费者流水线：

```text
Kingdom 本地读取
    -> 数据转换和序列化
    -> 按当前目标 payload 组装上传批次
    -> 有界 BlockingCollection 队列
    -> Web 上传 worker
    -> 指标采集和自适应控制器
    -> 调整后续读取及上传批次
```

控制器只能影响尚未生成的后续批次，不修改正在读取、排队或上传的请求。控制器自身失败时回到任务开始时的初始参数，不得中断同步。

实现使用项目现有能力：

- `BlockingCollection<T>`：有界队列和背压。
- `SemaphoreSlim` 或等效内部控制：动态降低实际并发。
- `Stopwatch`、`Interlocked`：指标采集。
- Newtonsoft.Json：人工配置和学习状态序列化。

不新增 Polly、TPL Dataflow 等第三方依赖。

## 4. 配置和状态文件

### 4.1 人工配置

文件名：

```text
AdaptiveSyncSettings.json
```

固定读取路径：

```text
<程序安装目录>\AdaptiveSyncSettings.json
```

规则：

- 与 EXE、`App.config` 位于同一目录。
- 程序只读，不创建、不修改、不回写。
- 每次 `SyncCommandAction` 开始时加载一次，任务执行期间不热更新。
- 文件不存在时使用配置模型的内置默认值。
- JSON 缺少字段时保留相应属性的内置默认值。
- JSON 格式错误时记录英文日志，整份使用内置默认值。
- 参数超出安全范围时进行规范化并记录英文日志。
- 同步性能参数不再从 `App.config` 读取。
- 学习状态兼容版本除外，固定读取 `App.config` 的 `appSettings["Version"]`。

推荐在带默认值的实例上使用 `JsonConvert.PopulateObject()`，确保部分配置可以自然继承代码默认值。

### 4.2 动态学习状态

文件名：

```text
AdaptiveSyncState.json
```

保存时依次尝试：

1. `<程序安装目录>\AdaptiveSyncState.json`
2. `%LOCALAPPDATA%\KindomDataAPIServer\Configs\AdaptiveSyncState.json`
3. `%TEMP%\KindomDataAPIServer\Configs\AdaptiveSyncState.json`

加载时检查以上位置存在且合法的状态文件。只有状态文件的 `Version` 与 `App.config` 中 `appSettings["Version"]` 的字符串值完全相同才可使用；版本不匹配时记录英文日志并忽略。程序在其余兼容文件中选择 `UpdatedAtUtc` 最新的一份。保存仍按上述顺序尝试。每次任务必须在日志和任务报告中记录实际加载路径与实际保存路径。应用版本缺失时不加载也不保存学习状态。

状态写入要求：

- 先写同目录临时文件，再进行原子替换，避免产生半截 JSON。
- 当前路径写入失败时记录英文日志并尝试下一个位置。
- 所有位置均失败时只记录错误，不影响同步结果。
- 四类数据独立更新；某一类失败不得阻止其他成功类型保存状态。

### 4.3 参数优先级

有效参数按以下顺序确定：

```text
人工配置的算法和安全边界
    -> 上次学习状态中的可复用初始值
    -> 人工配置中的初始值
    -> 配置模型的内置默认值
```

学习状态只能覆盖下一次任务的初始读取批量和初始 payload，不能覆盖：

- 最大读取批量
- 最大 payload
- 上传并发
- 评估窗口
- 增长、回退和冷却规则
- 各类型业务数据量上限

## 5. 精简配置结构

公共算法参数四类共用，每类只保留读取、上传和业务上限差异。

```json
{
  "enabled": true,
  "common": {
    "fastPayloadGrowthPercent": 75,
    "stablePayloadGrowthPercent": 25,
    "throughputImprovementPercent": 10,
    "rollbackAfterNoImprovementWindows": 2,
    "cooldownWindows": 2,
    "allowConcurrencyIncrease": false
  },
  "fixed": {
    "wellHeaderUploadBatchSize": 5000,
    "useFileImport": true
  },
  "dataTypes": {
    "formation": {
      "readBatch": {
        "initial": 20,
        "max": 100,
        "targetSeconds": 3
      },
      "upload": {
        "initialPayloadMiB": 1,
        "maxPayloadMiB": 64,
        "concurrency": 2,
        "stableEvaluationRequests": 4,
        "maxFormationCount": 100000
      }
    },
    "trajectory": {
      "readBatch": {
        "initial": 5,
        "max": 100,
        "targetSeconds": 2
      },
      "upload": {
        "initialPayloadMiB": 8,
        "maxPayloadMiB": 64,
        "concurrency": 3,
        "stableEvaluationRequests": 6,
        "maxPointCount": 2000000
      }
    },
    "production": {
      "readBatch": {
        "initial": 2,
        "max": 50,
        "targetSeconds": 3
      },
      "upload": {
        "initialPayloadMiB": 4,
        "maxPayloadMiB": 64,
        "concurrency": 3,
        "stableEvaluationRequests": 6,
        "maxDailyDataCount": 1000000
      }
    },
    "wellLog": {
      "readBatch": {
        "initial": 3,
        "max": 20,
        "targetSeconds": 3
      },
      "upload": {
        "initialPayloadMiB": 4,
        "maxPayloadMiB": 64,
        "concurrency": 5,
        "stableEvaluationRequests": 10,
        "maxSampleCount": 5000000
      }
    }
  }
}
```

以上数值中的 `initialPayloadMiB` 是冷启动或没有可用学习状态时的起始目标，`maxPayloadMiB` 和各类 `max*Count` 只是自适应增长的人工安全上限，不代表每个请求都会达到该值。当前配置和代码级绝对上限均为 64 MiB；这样既允许甲方高速内网继续向上探索，也避免错误数据、并发队列和失败重传产生不可控的内存及网络开销。`stableEvaluationRequests` 按各类型上传并发的两轮请求数设置，用来降低并发完成顺序对吞吐评估的干扰。

固定在代码中、不增加配置项的规则：

- 读取批量最小值为 1。
- 上传并发最小值为 1。
- 单次并发最多增加 1；第一版禁止自动增加。
- HTTP 保护触发时 payload 减半。
- 持续异常时并发减半。
- 参数不得突破人工配置和代码绝对上限。
- 业务 `failed` 不触发参数回退。

## 6. 上传 payload 自适应

### 6.1 组批原则

payload 字节数是主要限制，类型特有的数据量是第二重限制：

| 类型 | payload 计算 | 第二重限制 |
| --- | --- | --- |
| 分层 | `CalculateSize()` | 层位记录数 |
| 轨迹 | 与 `APIClient` 相同的 `JsonHelper.ToJson()` UTF-8 字节数 | 轨迹点数 |
| 生产 | 与 `APIClient` 相同的 `JsonHelper.ToJson()` UTF-8 字节数 | 日数据条数 |
| 测井 | `CalculateSize()` | 样点数 |

加入下一项将超过当前目标 payload 或业务上限时，提交当前批次。单条数据本身超过目标时允许单独上传，并在报告中标记为超大单项请求。

### 6.2 快速爬升阶段

- 每完成一轮当前类型配置的上传并发数后评估一次，即快速窗口请求数直接等于 `upload.concurrency`，不再单独配置。
- 没有 HTTP 保护信号且吞吐表现稳定时，目标 payload 增加 75%。
- 目标达到最大值或吞吐量不再改善时进入稳定优化阶段。
- 快速阶段队列容量等于上传并发数，减少旧的小批次积压，使新目标尽快生效。

### 6.3 稳定优化阶段

- 每种数据类型按自己的 `upload.stableEvaluationRequests` 统计成功请求数并评估一次；默认覆盖两轮配置并发。
- 窗口吞吐量比基准提升超过 10% 时，目标 payload 增加 25%。
- 单个窗口没有改善时保持当前值。
- 连续两个窗口没有改善时回退到上一个最佳稳定值。
- 回退后冷却两个评估窗口，冷却期间只允许保护性降低，不允许增长。
- 稳定阶段队列容量恢复为上传并发数的两倍。

窗口吞吐量按下式计算：

```text
窗口成功上传 payload 字节数 / 窗口实际墙钟时间
```

不能仅以单请求耗时作为带宽利用率依据。

### 6.4 立即保护

以下信号不等待评估窗口：

- HTTP 超时
- HTTP 429
- HTTP 5xx
- 网络重试
- 请求延迟相对近期基线显著恶化
- 进程内存超过代码安全阈值

处理顺序：

1. 当前目标 payload 减半，但不得低于代码安全下限。
2. 问题持续时实际上传并发减半，最低为 1。
3. 进入冷却窗口。

`APIClient` 需要向调用层提供结构化请求遥测，包括最终状态、是否重试、尝试次数和总耗时。不得通过解析文本日志驱动控制器。

## 7. 本地读取自适应

共用规则：

- 连续两个读取批次稳定、上传队列未满且没有内存压力时，读取批量增加 25%。
- 读取耗时超过目标、读取数据量超过类型限制或内存压力过高时，读取批量减半。
- 上传队列持续满时暂停增加读取批量，由现有有界队列进行背压。
- 每次循环必须按“已处理位置 + 当前动态批量”推进，不能使用固定步长导致跳过或重复 UWI。

类型差异：

- 分层：重点根据 Kingdom 查询耗时和原始层位记录数调整。
- 轨迹：读取较快，可在安全上限内较积极增长，同时观察轨迹点数。
- 生产：同时观察原始历史记录数和展开后的日数据量。
- 测井：同时观察曲线数、样点数、序列化耗时和内存，增长应最保守。

除 SDK 查询耗时外，还应单独记录数据转换、批次组装、序列化及队列等待耗时。甲方测井日志中总耗时明显大于读取和并发上传的简单最大值，不能忽略准备阶段成本。

## 8. 并发策略

第一版默认：

```text
allowConcurrencyIncrease = false
```

含义：

- 正常并发始终以人工配置值为上限。
- 控制器可以因 HTTP 或后端压力临时降低并发。
- 临时降低后的并发不写入学习状态。
- 维护人员需要提高并发时手工修改 `AdaptiveSyncSettings.json`。

未来开启自动增长时，必须同时满足：

- payload 已接近配置上限。
- 上传队列持续积压。
- 瓶颈明确位于上传端。
- HTTP 状态持续稳定。
- 已经过较长冷却期。

即便满足条件，每次也只能增加 1，且不得突破人工配置和代码绝对上限。

## 9. 跨任务学习

每类数据独立保存以下状态：

- 最佳稳定 payload
- 最佳稳定读取批量
- 最佳稳定吞吐量
- 有效评估窗口数
- `UpdatedAtUtc`
- 可用于区分服务端和 Kingdom 项目的环境标识

只有同时满足以下条件才更新某一类型的状态：

- 该类型完整同步结束。
- 任务未取消。
- 没有未处理异常。
- 已完成足够数量的评估窗口。
- 最终存在已确认的最佳稳定值。
- 保存值不是刚由 HTTP 保护产生的临时回退值。

下一次同步：

- 初始 payload 使用上次最佳稳定 payload，并限制在最新人工配置边界内。
- 初始读取批量使用上次最佳稳定批量的 75% 至 80%，取整后限制在最新边界内。
- 上传并发始终重新读取人工配置，不从学习状态恢复。

删除所有候选位置的 `AdaptiveSyncState.json` 后，下次任务从人工配置或代码默认值重新学习。

## 10. 任务报告要求

`SyncTaskReportService` 最终报告必须增加以下信息。

### 10.1 配置来源

- 人工配置期望路径
- 人工配置来源：JSON 或内置默认值
- JSON 加载、字段规范化错误
- 动态状态实际加载路径
- 动态状态实际保存路径
- 动态状态保存是否成功

### 10.2 每类数据参数

- 人工配置初始值和最大值
- 上次学习值
- 本次实际初始值
- 运行期间最小值、最大值和最终值
- 本次最佳稳定值
- 下次同步将使用的初始值
- 固定并发和运行期间最低实际并发

### 10.3 调整记录

每次调整记录：

- 数据类型
- 阶段：快速、稳定、冷却或保护
- 旧值和新值
- 调整原因
- 窗口请求数、payload 字节数和墙钟时间
- 当前吞吐量及相对基准变化
- 队列占用
- HTTP 保护信号

### 10.4 汇总指标

- 初始吞吐量和最佳吞吐量
- payload 增长、回退次数
- 读取批量增长、回退次数
- HTTP 保护次数
- 并发临时降低次数
- 超大单项请求数
- 读取、准备、序列化、队列等待、上传墙钟耗时

所有同步诊断继续使用英文。报告创建或写入失败不得中断同步。

## 11. 现有配置和界面清理

实施时删除原高级配置入口和管理逻辑：

- 删除高级配置菜单入口。
- 删除 `AdvancedSettingView` 及仅为该页面服务的代码。
- 删除页面对应的 EXE 配置写入逻辑。
- 将同步性能参数从 `App.config` 迁移到 JSON 模型。
- 删除不再使用的同步性能 appSettings 键和访问方法。

`App.config` 文件本身必须保留，用于程序集绑定重定向及其他非同步运行配置。

## 12. 实施建议

建议按以下顺序实现：

1. 增加配置模型、默认值、校验和人工配置加载器。
2. 增加动态状态模型、候选路径选择和原子写入。
3. 为 `APIClient` 增加结构化 HTTP 遥测。
4. 实现通用自适应控制器及四类策略上下文。
5. 改造四类上传组批逻辑，以 payload 字节为主限制。
6. 改造四类本地读取循环，使读取批量可以动态变化。
7. 扩展任务报告。
8. 删除高级配置页面、菜单和旧 `App.config` 同步参数。
9. 构建并进行小数据、甲方规模模拟、异常回退及状态恢复验证。

## 13. 验收标准

- 没有人工 JSON 时，四类同步使用代码默认值并正常完成。
- 部分 JSON 字段缺失时，缺失字段使用代码默认值。
- JSON 损坏时记录错误并使用默认值，不中断同步。
- 上传批次同时遵守 payload 和类型业务上限。
- 快速阶段每 2 个完成请求可调整，稳定阶段每 3 个完成请求可评估。
- 连续两个无改善窗口才回退，回退后冷却两个窗口。
- 业务 `failed` 不影响控制器，HTTP 超时、429、5xx 和重试可以触发保护。
- 第一版不会自动提高并发，但可以临时降低并发。
- 成功类型的最佳稳定参数可写入状态并用于下一次同步。
- 安装目录不可写时自动回退到 LOCALAPPDATA，再失败时回退到 TEMP。
- 报告能够还原配置来源、每次调整、优化结果和下一次起始参数。
- 自适应、配置、状态或报告发生内部错误时不影响同步主流程。
- Debug 解决方案构建成功，并完成四类数据的手工同步验证。
