<Window x:Class="KindomDataAPIServer.Views.LogDatasetCreateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:local="clr-namespace:KindomDataAPIServer.Views"
        mc:Ignorable="d" WindowStyle="ToolWindow"
        Title="New LogDataset" Height="130" Width="240">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" Height="Auto">
                <StackPanel.Resources>
                    <Style TargetType="StackPanel">
                        <Setter Property="Height" Value="30"></Setter>
                    </Style>
                </StackPanel.Resources>
                <StackPanel Orientation="Horizontal">
                    <Label Content="New Name" Width="Auto" VerticalAlignment="Center"></Label>
                    <dxe:TextEdit x:Name="txt" Width="150" Margin="5,0,0,0" ></dxe:TextEdit>
                </StackPanel>
            </StackPanel>
        </Grid>
        <dx:SimpleButton Grid.Row="1" Height="26" Content="Create" Click="SimpleButton_Click" Width="60" HorizontalAlignment="Right" Margin="0,0,1,0"></dx:SimpleButton>
    </Grid>
</Window>
