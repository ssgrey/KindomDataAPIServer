<dx:ThemedWindow
    x:Class="KindomDataAPIServer.Views.OpenProjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" Topmost="True"
    x:Name="root"  Icon="/datapool.ico" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="NoResize"
    Title="Open Project" Height="180" Width="300" >
    <Grid Margin="10" >
        <Grid x:Name="loginGrid" Visibility="{Binding LoginGridVisiable}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Margin="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Vertical" Margin="0,0,0,0" Height="Auto" HorizontalAlignment="Left">
                        <StackPanel.Resources>
                            <Style TargetType="StackPanel">
                                <Setter Property="Height" Value="30"></Setter>
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" >
                            <Label Content="Project Path" Width="80"></Label>
                            <dxe:ButtonEdit  Text="{Binding ProjectPath}" IsReadOnly="True" Width="180" Height="24" DefaultButtonClick="{DXEvent 'BrowseProjectPath()'}" ></dxe:ButtonEdit>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" >
                            <Label Content="DB Username" Width="80"></Label>
                            <dxe:TextEdit  Text="{Binding DBUserName,UpdateSourceTrigger=PropertyChanged}" NullText="Can be empty"  Width="180" Height="24" ></dxe:TextEdit>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" >
                            <Label Content="DB Password" Width="80"></Label>
                            <dxe:PasswordBoxEdit  Text="{Binding DBPassword,UpdateSourceTrigger=PropertyChanged}" NullText="Can be empty"  Width="180" Height="24" ></dxe:PasswordBoxEdit>
                        </StackPanel>

                    </StackPanel>
                </Grid>
            </Grid>
            <Grid Grid.Row="1">
                <dx:SimpleButton Content="Login" Height="26"  Width="55" Command="{Binding LoginCommand}"   HorizontalAlignment="Right"  Margin="0,0,5,0"></dx:SimpleButton>
            </Grid>
        </Grid>
        <Grid x:Name="loadGrid" >
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"></Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoginGridVisiable}" Value="Collapsed">
                            <Setter Property="Visibility" Value="Visible"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Margin="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Vertical" Margin="0,0,0,0" Height="Auto" HorizontalAlignment="Left">
                        <StackPanel.Resources>
                            <Style TargetType="StackPanel">
                                <Setter Property="Height" Value="30"></Setter>
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel Orientation="Horizontal" >
                            <Label Content="User：" VerticalContentAlignment="Center" Width="50"></Label>
                            <dxe:ComboBoxEdit Width="210" Height="23"  EditValue="{Binding LoginName,UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Authors}"  IsTextEditable="False"></dxe:ComboBoxEdit>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Grid>
            <Grid Grid.Row="1">
                <dx:SimpleButton Content="Load" Height="26" Width="55" Command="{Binding LoadCommand}" Click="Load_Click" HorizontalAlignment="Right"  Margin="0,0,5,0"></dx:SimpleButton>
            </Grid>
        </Grid>
    </Grid>

</dx:ThemedWindow>
